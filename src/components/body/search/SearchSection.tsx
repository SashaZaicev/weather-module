import {FC} from 'react';
import style from './searchSection.module.scss'

export const SearchSection: FC<any> = ({}) => {
  return (
    <div className={style.searchSection}>
      search
    </div>
  );
};
